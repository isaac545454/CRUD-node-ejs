<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.0-beta1/dist/css/bootstrap.min.css"
     rel="stylesheet" integrity="sha384-0evHe/X+R7YkIZDRvuzKMRqM+OrBnVFBL6DOitfPri4tjfHxaWutUpFmBp4vmVor"
      crossorigin="anonymous">
    <title>redirerection</title>
  </head>
  <body>
    <div class="container w-50 card p-4 mt-5">
     <div class="d-flex justify-content-center align-items-center flex-column m-2">
    <h1>ola mundo</h1>
    <a class="text-info" href="/">links=></a>
    </div>
    <form class="d-flex flex-column" action="/edit/<%=  body.id %>" method="post">
        <input class="mb-2" name="titulo" placeholder="titulo" value="<%=  body.titulo %>" >
        <input class="mb-2" name="descrição" placeholder="descrição" value="<%=  body.descrição %>">
        <input class="mb-4" name="url" placeholder="url" value="<%=  body.url %>" required>
        <button class="btn btn-primary">edit</button>
        <% if (error){ %> 
          <div class="alert alert-danger mt-3"><strong> error:<%= error.message %> </strong>
          </div>
        <% } %> 
    </form>
   </div>

  </body>
</html>